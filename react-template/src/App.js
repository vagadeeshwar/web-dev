import styled from "styled-components/macro";
import Flex from "./Components/Card";

export default function App() {
  return (
    <Container forwardedAs="main" type="global">
  
    </Container>
  );
}

const Container = styled(Flex)``;
